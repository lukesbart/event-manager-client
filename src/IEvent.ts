export type IEvent = {
    id: number,
    event_title: string,
    date: string,
    description: string,
    video_url: string,
    audio_url: string,
    handout_url: string,
}

export default IEvent;